export class AudioEngine {
  constructor(){this.volume=.45;this.ctx=null;}
  start(){if(!this.ctx){this.ctx=new AudioContext();this.master=this.ctx.createGain();this.master.gain.value=this.volume;this.master.connect(this.ctx.destination);this.engine=this.ctx.createOscillator();this.engine.type='sawtooth';this.engineGain=this.ctx.createGain();this.engineGain.gain.value=0;const filter=this.ctx.createBiquadFilter();filter.frequency.value=130;this.engine.connect(filter);filter.connect(this.engineGain);this.engineGain.connect(this.master);this.engine.start();}this.ctx.resume();}
  setVolume(v){this.volume=v;if(this.master)this.master.gain.value=v;}
  drive(speed,active){if(!this.ctx)return;this.engine.frequency.setTargetAtTime(28+Math.abs(speed)*2.3,this.ctx.currentTime,.1);this.engineGain.gain.setTargetAtTime(active?.018+Math.abs(speed)*.0015:0,this.ctx.currentTime,.15);}
  boom(strength=1,distance=0){if(!this.ctx)return;const c=this.ctx,t=c.currentTime,g=c.createGain(),f=c.createBiquadFilter(),b=c.createBuffer(1,c.sampleRate*.8,c.sampleRate),data=b.getChannelData(0);for(let i=0;i<data.length;i++)data[i]=(Math.random()*2-1)*Math.exp(-i/data.length*5);const n=c.createBufferSource();n.buffer=b;f.type='lowpass';f.frequency.setValueAtTime(1600,t);f.frequency.exponentialRampToValueAtTime(90,t+.6);g.gain.value=Math.min(.7,strength*.32)/(1+distance*.045);n.connect(f);f.connect(g);g.connect(this.master);n.start();n.stop(t+.8);n.onended=()=>{n.disconnect();f.disconnect();g.disconnect();};const o=c.createOscillator(),og=c.createGain();o.frequency.setValueAtTime(100,t);o.frequency.exponentialRampToValueAtTime(24,t+.25);og.gain.setValueAtTime(g.gain.value*.8,t);og.gain.exponentialRampToValueAtTime(.001,t+.3);o.connect(og);og.connect(this.master);o.start();o.stop(t+.32);o.onended=()=>{o.disconnect();og.disconnect();};}
  click(){if(!this.ctx)return;const o=this.ctx.createOscillator(),g=this.ctx.createGain();o.frequency.value=520;g.gain.setValueAtTime(.08,this.ctx.currentTime);g.gain.exponentialRampToValueAtTime(.001,this.ctx.currentTime+.06);o.connect(g);g.connect(this.master);o.start();o.stop(this.ctx.currentTime+.07);o.onended=()=>{o.disconnect();g.disconnect();};}
}
