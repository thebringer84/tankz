import test from 'node:test';import assert from 'node:assert/strict';import * as THREE from 'three';import RAPIER from '@dimforge/rapier3d-compat';import {TerrainRuts} from '../src/terrain-ruts.js';import {terrainSampler} from '../src/grounding.js';
await RAPIER.init();
test('soft ruts depress mesh and collision surface together, remain bounded and preserve hard surfaces',()=>{const world=new RAPIER.World({x:0,y:-18,z:0}),geo=new THREE.PlaneGeometry(220,220,180,180).rotateX(-Math.PI/2),ruts=new TerrainRuts(geo,world),sample=terrainSampler(geo,220,180);for(let i=0;i<80;i++)ruts.stamp(new THREE.Vector3(0,0,0),0,1.24,1);ruts.update(.2);world.step();const x=220/180,depth=ruts.sample(x,0),height=sample(x,0);assert.ok(depth>.02&&depth<=.075);assert.ok(height<-.02&&height>=-.07501);const hit=world.castRay(new RAPIER.Ray({x,y:2,z:0},{x:0,y:-1,z:0}),4,true);assert.ok(hit);assert.ok(Math.abs(2-hit.timeOfImpact-height)<.001);ruts.ruins=[[0,0]];const before=ruts.data.slice();ruts.stamp(new THREE.Vector3(0,0,0),0,1.24,1);assert.deepEqual(ruts.data,before);ruts.dispose();world.free();});
