import {chromium} from '@playwright/test';import assert from 'node:assert/strict';
const browser=await chromium.launch({channel:'chrome',headless:true,args:['--enable-webgl']});const page=await browser.newPage({viewport:{width:1440,height:900}}),errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});
try{await page.goto('http://localhost:5173');await page.waitForFunction(()=>window.tankz?.game.running);await page.locator('[data-action="deploy"]').click();await page.locator('[data-action="launch-mission"]').click();await page.keyboard.press('KeyQ');await page.waitForTimeout(150);assert.equal(await page.evaluate(()=>tankz.game.smokeGrenades.items.length),6);assert.equal(await page.evaluate(()=>tankz.game.smokeClouds.length),0);await page.screenshot({path:'test-artifacts/smoke-grenades-launch.png'});await page.keyboard.press('KeyQ');assert.equal(await page.evaluate(()=>tankz.game.smokeGrenades.items.length),6);await page.waitForTimeout(1800);assert.equal(await page.evaluate(()=>tankz.game.smokeGrenades.items.length),0);assert.equal(await page.evaluate(()=>tankz.game.smokeClouds.length),6);await page.screenshot({path:'test-artifacts/smoke-screen.png'});
await page.keyboard.down('KeyW');await page.keyboard.down('Space');await page.waitForTimeout(3300);await page.screenshot({path:'test-artifacts/dust-trail.png'});await page.keyboard.up('KeyW');await page.keyboard.up('Space');assert.ok(await page.evaluate(()=>tankz.game.fx.particles.some(p=>p.trail)));assert.deepEqual(errors,[]);console.log('Q launches six physical grenades, cooldown prevents duplicates, bursts create smoke cover, and driving emits continuous dust.');}finally{await browser.close();}
